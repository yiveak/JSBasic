/* 
使用 indexOf 统计一个字符串中某个字母出现的次数
*/

function countCh(str, target) {
    let count = 0;

    if(str.indexOf(substr)!==-1){
        count++;
    }
    return count;
}

let str = 'To be, or not to be, that is the question.';